
export function user(state={current: null, error:null}, {type, payload}) {
    switch (type) {
        case 'LOGIN':return state
            
            
    
        default:
            return state
    }
  }